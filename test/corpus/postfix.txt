=========
factorial
=========

x!

---

(source_file
  (statement
    (parse_tree
      (postfix
        (parse_tree
          (token
            (identifier)))))))

=============
sum of twists
=============

x(*)

---

(source_file
  (statement
    (parse_tree
      (postfix
        (parse_tree
          (token
            (identifier)))))))

============
upper shriek
============

x^!

---

(source_file
  (statement
    (parse_tree
      (postfix
        (parse_tree
          (token
            (identifier)))))))

========
pullback
========

x^*

---

(source_file
  (statement
    (parse_tree
      (postfix
        (parse_tree
          (token
            (identifier)))))))

==============
sheafification
==============

x^~

---

(source_file
  (statement
    (parse_tree
      (postfix
        (parse_tree
          (token
            (identifier)))))))

============
lower shriek
============

x_!

---

(source_file
  (statement
    (parse_tree
      (postfix
        (parse_tree
          (token
            (identifier)))))))

===========
pushforward
===========

x_*

---

(source_file
  (statement
    (parse_tree
      (postfix
        (parse_tree
          (token
            (identifier)))))))

===============
subscript-tilde
===============

x_~

---

(source_file
  (statement
    (parse_tree
      (postfix
        (parse_tree
          (token
            (identifier)))))))

=====
tilde
=====

x~

---

(source_file
  (statement
    (parse_tree
      (postfix
        (parse_tree
          (token
            (identifier)))))))

========================
postfix w/ strong binary
========================

x.y_*

---

(source_file
  (statement
    (parse_tree
      (postfix
        (parse_tree
          (binary
            (parse_tree
              (token
                (identifier)))
            (parse_tree
              (token
                (identifier)))))))))

======================
postfix w/ weak binary
======================

x + y!

---

(source_file
  (statement
    (parse_tree
      (binary
        (parse_tree
          (token
            (identifier)))
        (parse_tree
          (postfix
            (parse_tree
              (token
                (identifier)))))))))
